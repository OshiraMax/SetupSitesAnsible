server {
    listen 80;
    server_name {{ item.domain }} www.{{ item.domain }};
    root {{ item.root }};
    index index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }

    location /uploads/ {
        alias {{ item.uploads }};
    }
}
